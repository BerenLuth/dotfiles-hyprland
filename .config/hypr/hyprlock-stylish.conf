# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hyprlock
# Original config submitted by https://github.com/SherLock707

# Sourcing colors generated by wallust
source = $HOME/.cache/wal/colors-hyprland.conf
source = ~/.config/hypr/custom/variables.conf   # we need this to access $mainMonitor
$Scripts = $HOME/.config/hypr/scripts

general {
    grace = 1
    ignore_empty_input = true
    enable_fingerprint = true
}

auth {
    fingerprint:enabled = true
    fingerprint:ready_message =
    fingerprint:present_message =「 Welcome back, $USER 」
}

background {
    monitor =
	#path = screenshot   # screenshot of your desktop
	path = $HOME/.config/hypr/wallpaper/mountain.jpg  # NOTE only png supported for now
    #color = $color7

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_size = 0
    blur_passes = 0 # 0 disables blurring
    noise = 0
    contrast = 1.000 # Vibrant!!!
    brightness = 0.7000
    vibrancy = 0.02100
    vibrancy_darkness = 0
}

input-field {
    monitor =
    size = 180, 32
    outline_thickness = 2
    dots_size = 0.28 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = $color5
    inner_color = $color0
    font_color = $color12
    font_size = 22
    fade_on_empty = true
    placeholder_text = <i>Speak, friend, and enter</i> # Text rendered in the input box when it's empty.
    hide_input = false
    rounding = 8
    invert_numlock = true

    position = 0, 80
    halign = center
    valign = bottom
    zindex = 10
}

# Date
# label {
#     monitor =
#     text = cmd[update:18000000] echo "<b> "$(date +'%A, %-d %B %Y')" </b>"
#     color = $color12
#     font_size = 25
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1
#     position = 0, 360
#     halign = center
#     valign = center
# }

label {
    monitor =
    text = cmd[update:18000000] echo "<b> "$(date +'%A')" </b>"
    color = $color12
    font_size = 100
    font_family = Geist Mono 10
    shadow_passes = 3
    shadow_size = 1
    position = 0, 0
    halign = right
    valign = top
}
label {
    monitor =
    text = cmd[update:18000000] echo "<b> "$(date +'%-d %B %Y')" </b>"
    color = $color12
    font_size = 40
    font_family = Geist Mono 10
    shadow_passes = 3
    shadow_size = 1
    position = -40, -175
    halign = right
    valign = top
}

# Time
# Hours
# label {
#     monitor =
#     text = cmd[update:1000] echo "<b><big> $(date +"%H") </big></b>"
#     color = $color1
#     font_size = 150
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1

#     position = 0, 100
#     halign = center
#     valign = center
# }

# Minutes
# label {
#     monitor =
#     text = cmd[update:1000] echo "<b><big> $(date +"%M") </big></b>"
#     color = $color1
#     font_size = 150
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1

#     position = 0, -75
#     halign = center
#     valign = center
# }

label {
    monitor =
    text = cmd[update:1000] echo "<b><big> $(date +"%H:%M") </big></b>"
    color = $color1
    font_size = 400
    font_family = Geist Mono 10
    shadow_passes = 3
    shadow_size = 1

    position = 0, 450
    halign = center
    valign = center
    zindex = 0
}

# User
label {
    monitor =
    text =  $USER
    color = $color12
    font_size = 16
    font_family = MesloLGS NF
    shadow_passes = 3
    shadow_size = 1

    position = 0, 130
    halign = center
    valign = bottom
    zindex = 10
}

###############
# FINGERPRINT #
###############

# fingerprint icon label, print only if fprintd is installed
label {
    monitor =
    text = cmd[update:18000000] type fprintd-verify &> /dev/null && echo "〉󰈷〈"
    color = $color1
    font_size = 16
    font_family = MesloLGS NF
    shadow_passes = 3
    shadow_size = 1

    position = 0, 30
    halign = center
    valign = bottom
    zindex = 10
}

# fingerprint prompt (comes from hyprlock auth)
label {
    monitor =
    text = $FPRINTPROMPT
    color = $color2
    font_size = 10
    font_family = MesloLGS NF
    position = 0, 10
    halign = center
    valign = bottom
    zindex = 10
}

#########
# MUSIC #
#########

# music cover
# image {
#     monitor =
#     path = /tmp/playerctl-cover.jpg
#     size = 150
#     rounding = 8
#     border_size = 0
#     border_color = $color1
#     rotate = 0
#     reload_time = 1
#     position = -30, -30
#     halign = right
#     valign = top
# }

# Music status (rendered over the cover art)
# label {
#     monitor =
#     text = cmd[update:1000] ~/.config/hypr/scripts/hyprlockMusic.sh status
#     color = $color7
#     font_size = 14
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1
#     position = -45, -150
#     halign = right
#     valign = top
# }

# # Music title (and triggers cover art download)
# label {
#     monitor =
#     text = cmd[update:1000] ~/.config/hypr/scripts/hyprlockMusic.sh
#     color = $color12
#     font_size = 16
#     font_family = Geist Mono 10
#     position = -200, -70
#     halign = right
#     valign = top
# }

# # Music artist
# label {
#     monitor =
#     text = cmd[update:1000] ~/.config/hypr/scripts/hyprlockMusic.sh artist
#     color = $color1
#     font_size = 16
#     font_family = Geist Mono 10
#     position = -200, -100
#     halign = right
#     valign = top
# }

###########
# WEATHER #
###########

# wttr.in weather
# label {
#     monitor =
#     text = cmd[update:18000000] echo "<b><big> $(curl wttr.in/vittorio-veneto?format=1)</big></b>"
#     color = $color13
#     font_size = 15
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1

#     position = -30, 30
#     halign = right
#     valign = bottom
# }


# label {
#     monitor =
#     text = cmd[update:18000000] echo "$(~/.config/hypr/scripts/hyprlockBluetooth.sh)"
#     color = $color13
#     font_size = 15
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1

#     position = 30, 60
#     halign = left
#     valign = bottom
#     zindex = 2
# }

# label {
#     monitor =
#     text = cmd[update:18000000] echo "$(~/.config/hypr/scripts/hyprlockNetwork.sh)"
#     color = $color13
#     font_size = 15
#     font_family = Geist Mono 10
#     shadow_passes = 3
#     shadow_size = 1

#     position = 30, 30
#     halign = left
#     valign = bottom
#     zindex = 2
# }


label {
    monitor =
    text = cmd[update:18000000] echo "<b><big>$(curl wttr.in/vittorio-veneto?format=1)</big></b>"
    color = $color13
    font_size = 15
    font_family = Geist Mono 10
    shadow_passes = 3
    shadow_size = 1

    position = 30, -30
    halign = left
    valign = top
    zindex = 2
}



# Status info


# Put a picture of choice here. Default is the current wallpaper
image {
    monitor =
    path = ~/.config/hypr/wallpaper/mountain-fg.png
    size = 1800
    rotate = 0
    rounding = 0
    reload_time = -1
    border_size = 0
    position = 0,0
    halign = left
    valign = bottom
    zindex = 1
}
